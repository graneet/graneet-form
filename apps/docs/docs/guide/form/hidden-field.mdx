import {
    IFrame
} from "../../../src/components/IFrame";

# Hidden Field

You can create a hidden field (a field without a visible UI) using the 'HiddenField' component. This is useful when you want to store a value in the library (accessible through form.getFormValues(), for example) without displaying a UI component to the user.

## Example

<IFrame model="hidden-field-form" />
